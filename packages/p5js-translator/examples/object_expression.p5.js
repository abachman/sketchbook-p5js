// test handling of p5js constants as Property in ObjectExpression

function draw() {
  const setting = { width, height: height / 2, len: width + 2 }

  const model = []
  let next = model.slice(0)
}

